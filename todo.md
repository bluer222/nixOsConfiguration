Note to agent: avoid gnome and gtk apps like the plague. prefer qt or niri-native tools (generally qt; compositor-native if clearly better than the qt option)

 todo:
 - open games on desk 2(lutris, steam etc)
 - add color picker, trigger with super+c
 - kwallet not auto unlocked
 - add kactivitymanagerd(or whatevers the minimal to get it working) for dolphin recents
 - fourth desktop shows up in the waybar switcher(its suposed to be hidden)
 - use wpctl instead of pactl for audio.go
- make niri run on integrated gpu not nvidia(possibly already fixed, need restart to check)

not currenty posible without custom implementation:
 - chromakey based window transparency
 - niri shake-to-enlarge cursor(added to niri but not yet in nixpkgs)
 - add three finger zoom (waiting for niri zoom IPC)

